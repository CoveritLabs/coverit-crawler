repos:
  - repo: local
    hooks:
      - id: ruff
        name: ruff (format + check)
        entry: uv run python scripts/precommit_ruff.py
        language: system
        pass_filenames: true
        types: [python]
